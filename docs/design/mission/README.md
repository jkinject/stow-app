# 미션 카드 시안 (2026-09-02)

gpt-image-2 로 뽑은 세 방향. `prompts.py` 로 다시 뽑거나 변형할 수 있다.

| 파일 | 결 | 채택 |
|---|---|---|
| `warm-stamp.png` | 크림 카드 · 주황 도장 (커피숍 스탬프 카드) | **라이트 테마** |
| `dark-neon.png` | 진한 남색 · 강조색 도장 · 이어진 선 | **다크 테마** |
| `paper-tag.png` | 종이 라벨 · 얇은 이중 테두리 | 미채택 |

⚠ 이 그림들은 **참고용**이다. 앱에는 `src/components/DailyMission.tsx` 로 다시 그렸다 —
  래스터를 박으면 라이트/다크 전환도, 진행에 따른 상태 변화도 못 한다.

⚠ 색은 컴포넌트가 아니라 팔레트(`lib/theme.ts` 의 `Palette.mission`)에 있다.
  테마가 하나 늘 때 컴포넌트를 다시 열지 않으려고 그렇게 뒀다.
